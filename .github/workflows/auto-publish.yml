name: Auto Publish News

on:
  schedule:
    # Runs twice daily: 09:00 UTC (morning) and 18:00 UTC (evening)
    - cron: '0 9,18 * * *'
  workflow_dispatch: # Allow manual trigger from GitHub Actions UI

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run Grok Publisher
        env:
          XAI_API_KEY: ${{ secrets.XAI_API_KEY }}
          SANITY_API_TOKEN: ${{ secrets.SANITY_API_TOKEN }}
          NEXT_PUBLIC_SANITY_PROJECT_ID: zzzwo1aw
          NEXT_PUBLIC_SANITY_DATASET: production
        run: node scripts/grok-publish.js --count=2

      - name: Inject Internal Links
        env:
          SANITY_API_TOKEN: ${{ secrets.SANITY_API_TOKEN }}
          NEXT_PUBLIC_SANITY_PROJECT_ID: zzzwo1aw
          NEXT_PUBLIC_SANITY_DATASET: production
        run: node scripts/add-internal-links.js --hours=1
